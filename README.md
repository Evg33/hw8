# hw8
```
sudo apt update
sudo apt install ansible git python-pip
sudo pip install docker-compose
mkdir hw8
cd hw8
#git clone --branch dev git@github.com:Evg33/hw8.git .
git clone --branch main https://github.com/Evg33/hw8.git .
ansible all -m ping
./pb.yml
```
